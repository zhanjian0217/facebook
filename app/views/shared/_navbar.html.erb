<nav class='flex items-center justify-between w-auto bg-white h-14 '>
  <div class='flex items-center w-1/3 px-2 ml-2'>
    <%= link_to authenticated_root_path do %>
      <i class="facebook_icon fa-brands fa-facebook"></i>
    <% end %>
    <div class='flex items-center w-1/2 h-full ml-3 text-center text-gray-600 bg-gray-200 rounded-full'>
      <i class="py-3 mx-3 fa-solid fa-magnifying-glass"></i>
      <span class=''>
        搜尋 Facebook
      </span>
    </div>
  </div>
  <div class='flex items-center px-3'>
    <% if current_user %>
      <%= link_to('登出', destroy_user_session_path, method: :delete, class: '' ) %> 
    <% else %>
      <%= link_to('註冊', new_registration_path(:user)) %> |
      <%= link_to('登入', new_session_path(:user)) %>
    <% end %>
    <div class='ml-3 font-bold text-green-500'>
      <%= current_user.username if current_user %>
    </div>
  </div>
</nav>